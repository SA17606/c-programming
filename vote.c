//program to check a persons eligibility to vote
#include <stdio.h>
int main()
{
  int age, birth,kenya;
  printf ("enter age in years and birth:");
  scanf ("%d%d",&age,&birth);
  if (age>=18, birth=kenya){
    printf ("eligible to vote");} 
  else
  {printf ("not eligible to vote");}
  return 0;
}
  